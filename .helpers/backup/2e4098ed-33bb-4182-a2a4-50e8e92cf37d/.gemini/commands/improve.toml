# AUTO-GENERATED by clai-helpers — DO NOT EDIT. Source: .claude/commands/improve.md
description = "improve"

prompt = """
# Session Review & Self-Improvement

Review this conversation. Extract actionable improvements in three categories:

## 1. New Rules (mistakes → prevention)

For each mistake or correction the user made:

- Identify the root cause (wrong assumption, missing context, bad pattern)
- Determine the **right target file** for the rule:

| Rule Type                       | Target File                                                    |
| ------------------------------- | -------------------------------------------------------------- |
| Coding pattern / anti-pattern   | `.github/instructions/coding/copilot-instructions.md`          |
| Commit convention               | `.github/instructions/coding/commit-conventions.md`            |
| Script engine behavior          | `docs/SCRIPTS_GUIDE.md`                                        |
| Infrastructure / Docker         | `.github/instructions/project/scripts/copilot-instructions.md` |
| Production / deployment         | `docs/PRODUCTION-CHECKLIST.md`                                 |
| Security                        | `docs/SECURITY_CHECKLIST.md`                                   |
| Project architecture            | `docs/PROJECT_SPECIFIC_GUIDE.md`                               |
| AI agent behavior / preferences | Memory file (`feedback_*.md`)                                  |

**Do NOT add rules to CLAUDE.md** — it is a router, not a rulebook.

## 2. Validated Patterns (what worked well)

Identify non-obvious approaches that the user confirmed or accepted without pushback. Save as `feedback` memory so future sessions reuse them.

## 3. Repeated Workflows (candidates for automation)

If the same sequence of commands/tools was used 3+ times:

- Propose a new `/command` in `.claude/commands/`
- Or propose a hook in `settings.json`

## Output Format

```markdown
### Rules to Add
- [ ] [target file]: [rule description] — triggered by: [what went wrong]

### Patterns to Remember
- [ ] [memory file]: [pattern description] — confirmed by: [user action]

### Automation Candidates
- [ ] [command/hook name]: [workflow description] — repeated N times
```

Apply approved changes immediately. Ask before creating new files.

"""