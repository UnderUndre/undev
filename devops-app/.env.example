# Database
DATABASE_URL=postgresql://dashboard:changeme@localhost:5432/dashboard
POSTGRES_PASSWORD=changeme

# Auth (single admin user)
ADMIN_USER=admin
# Generate hash: node -e "import('bcrypt').then(b => b.hash('your-password', 10).then(console.log))"
ADMIN_PASSWORD_HASH=

# Telegram notifications (optional)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Server
PORT=3000
NODE_ENV=development
